name: 🔖 Echo release  

on:
  push:
    branches: ['main']

env:
    REGISTRY: ghcr.io
    IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      # - name: 🐳 Docker meta
      #   id: docker_meta
      #   uses: docker/metadata-action@v5
      #   with:
      #     images: esterlev/bussines

      # - name: ⬆️ Echo Release latest
      #   run: echo "docker tags ${{steps.docker_meta.outputs.tags}}"
        
        
      - name: 🔌Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
  
      # - name: :building_construction:Build Image
      #   run: |
      #       docker build . -t ghcr.io/skyvar/hawkeye360:latest
      #       docker push  ghcr.io/skyvar/hawkeye360
            
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          tags: ghcr.io/skyvar/hawkeye360:latest
          # labels: ${{ steps.docker_meta.outputs.labels }}
